# Tools

- `Paperclip API` at `http://127.0.0.1:3100`
  - Use bearer auth from `PAPERCLIP_API_KEY`.
  - Include `X-Paperclip-Run-Id: $PAPERCLIP_RUN_ID` on all mutating requests.
- `codex mcp list`
  - Confirms globally enabled MCP servers for Codex Local.
  - On March 11, 2026, `pencil` was confirmed enabled here without per-agent adapter changes.

version: 2
updates:
  # Docker ecosystem entry removed: watched ghcr.io/zeroclaw-labs/zeroclaw
  # which is defunct (GitHub suspended + ZeroClaw renamed to OpenClaw).
  # Limbo now uses OpenClaw as an npm dependency, not a Docker base image.
